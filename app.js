const randomNumber = (num) => {
    Math.floor(Math.random) * num;
}

const randomGift = {
    type: ['Pokemon', 'Pen', 'Power Ranger', 'Bag'],
    color: ['Red', 'Gold', 'Purple', 'Blue', 'White', 'Black'],
    pureness: ['45%', '55%', '65%', '100%']
};